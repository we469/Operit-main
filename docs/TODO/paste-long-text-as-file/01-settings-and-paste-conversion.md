# 设置与粘贴转换

## 现状

聊天输入的文本变更和附件添加分属不同路径，尚未识别粘贴操作，也没有超长文本阈值设置。

## 改动

- 在聊天设置中增加启用开关和字符阈值，默认阈值为 3000
- 为编辑器提供粘贴事件处理，比较粘贴前后的新增区间
- 将超过阈值的纯文本写入清理目录，并作为 `text/plain` 附件加入待发送列表
- 保持普通文本编辑、手动文件选择和已有附件发送路径不变

## 预期

用户粘贴超过阈值的文本后，输入框不会承载该文本，附件预览中会出现可发送的 `.txt` 文件。

[DONE]
