# 快速配置校验与界面 [DONE]

## 旧实现

快速配置将任意非空文本视为 API Key，并通过会话级布尔状态隐藏引导。自定义入口是低辨识度的文字按钮。

## 修改范围

- 提取无网络依赖的 API Key 规范化与格式判定
- 保存前拒绝中文、空白、控制字符和不可见字符
- 将自定义入口改为带调节图标的全宽描边按钮
- 更新首次配置文案与输入错误状态
- 让引导显示状态跟随实际对话配置

## 完成标准

合法的可打印半角 API Key 可以保存，错误输入会获得明确提示，快速配置和其他模型配置具有清晰的主次操作层级。
