# 自定义配置返回事务 [DONE]

## 旧实现

首次引导与普通设置共用同一个模型配置路由。新建或选择配置只改变页面局部状态，不会绑定到对话功能；页面销毁时的异步保存也无法保证返回前落盘。

## 修改范围

- 增加只供首次引导使用的模型配置路由
- 为当前路由实例注册统一的异步返回处理
- 返回前刷新最新表单状态并检查所选配置
- 将有效配置绑定到 `FunctionType.CHAT`
- 保存、校验或绑定失败时阻止退出并显示错误

## 完成标准

顶部返回、系统返回和返回手势都只在保存及绑定成功后离开页面；普通模型配置路由不改变功能绑定。
