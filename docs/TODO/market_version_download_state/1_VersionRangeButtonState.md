# 版本范围按钮状态

旧实现：

- 列表已定义 `Unavailable(Warning)` 下载入口状态，但没有为版本范围调用它
- 详情页仅禁用下载按钮，仍保留普通主操作的图标和色彩

调整：

- 以 `BuildConfig.VERSION_NAME` 和最新插件版本的最小、最大应用版本判断是否超出范围
- 超出范围时，列表使用警告不可用图标，详情页使用警告图标和错误色禁用按钮

预期结果：

用户在进入详情页前即可识别不兼容的插件，详情页也会保持一致的风险提示。

[DONE]
