# 执行边界与持久化状态

## 旧实现

`triggerWorkflow` 在调用执行器之前写入 `RUNNING`。其异常分支没有区分执行器是否已经返回结果，直接把状态更新为 `FAILED`。

## 修正意图

将最终状态和统计更新绑定到包含 `WorkflowExecutionRecord` 的执行结果。启动执行器之前发生的异常也要生成带失败阶段和原始异常的运行记录。

## 预期实现

- 定时和手动入口共用同一条执行结果持久化路径
- 每次最终 `SUCCESS` 或 `FAILED` 状态均有对应运行记录
- 运行环境初始化失败可被定时入口识别并延迟重试

[DONE]
