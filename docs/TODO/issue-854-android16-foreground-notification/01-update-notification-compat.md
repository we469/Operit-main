# 更新通知兼容层

## 旧实现

应用依赖 `androidx.core:core-ktx` 1.12.0。该依赖提供前台服务通知使用的
`NotificationCompat` 实现，但其发布时仅对齐到 Android 14。

## 修正意图

将依赖升级到 Android 16 对齐的稳定版本 1.18.0，使通知由当前平台兼容实现构建。

## 预期结果

Android 16 在发布 AI 前台服务通知时不再收到失效的数组资源引用，原有通知频道和操作保持不变。

## 验证

本次未执行构建或测试，遵循仓库执行准则。发布前应在 Android 16 设备上重复 Issue 854 的分享附件后连续发送两条消息流程。

[DONE]
