# 设置与提示词接入

旧实现：

- 记忆库设置只保存检索权重、自动保存间隔和嵌入模型配置
- 自动保存与手动更新都会进入 `MemoryLibrary.generateAnalysis`，但知识图谱提示词不接受用户规则

意图修正：

- 在当前记忆空间的偏好中保存文本规则
- 在自动保存设置区的间隔说明下方提供多行编辑框
- 在知识图谱提取提示词中，将规则限定为入库范围、分类和表达偏好，继续要求严格 JSON 输出

预期结果：

- 切换记忆空间时显示各自的规则
- 自动保存与手动更新使用相同规则
- 恢复默认清空该记忆空间的规则

[DONE]
