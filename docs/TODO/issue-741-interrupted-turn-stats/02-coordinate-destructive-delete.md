# 协调删除会话与取消

## 旧实现

Web 接口发现目标会话仍在输出时，会异步请求取消并立即删除数据库会话。中断收尾开始写入部分消息后，这两个操作可能交错，造成消息写入已删除会话。

## 修正

Web 删除接口等待总结与消息处理完成丢弃式取消，再执行会话删除。丢弃式取消不持久化部分回复。

## 验收

- 删除活动会话时，取消任务先结束，随后删除聊天记录。
- 中断统计的普通保存路径不参与破坏性删除。

[DONE]
